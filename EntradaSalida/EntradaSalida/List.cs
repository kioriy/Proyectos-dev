public enum messageResponse 
{
    connectionFail,
    actionFail,
    actionSuccess,
    fieldsRequired,
    allActionFail,
}
public enum dbAction
{
    insert,
    select,
    update,
    delete,
    free,
    multiInsert
}