public enum messageResponse
{
    connectionFail,
    actionFail,
    actionSuccess,
    fieldsRequired,
    allActionFail,
}
public enum logStatus
{
   
    FaltaFotografia,
    RegistroNoEncontrado,
}
public enum dbAction { 
    insert,
    select,
    update,
    delete,
    free,
    multiInsert
}